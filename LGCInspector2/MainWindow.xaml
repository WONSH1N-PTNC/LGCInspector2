<Window x:Class="LGCInspector2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:vm="clr-namespace:LGCInspector2.ViewModels"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LGCInspector2"
        mc:Ignorable="d"
        Title="LGCInspector2 (Mixed Cam)" Height="350" Width="700">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="현장 검증용 통합 분류기 (Cam1 &amp; Cam2)" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="혼합 데이터 폴더 선택:" FontWeight="Bold" Margin="0,0,0,5"/>
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Text="{Binding SourceDir}" IsReadOnly="True" VerticalContentAlignment="Center" Height="30" Padding="5"/>
            <Button Content="폴더 선택" Command="{Binding SelectFolderCommand}" Grid.Column="1" Width="80" Margin="5,0,0,0"/>
        </Grid>

        <Button Grid.Row="3" Content="검사 시작" Command="{Binding RunInspectionCommand}" 
                Height="50" FontSize="16" FontWeight="Bold" Background="#007ACC" Foreground="White"/>

        <StackPanel Grid.Row="4" VerticalAlignment="Bottom" Margin="0,15,0,0">

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,5">
                <TextBlock Text="경과 시간: " FontWeight="Bold" FontSize="14"/>
                <TextBlock Text="{Binding ElapsedTime}" FontWeight="Bold" FontSize="14" Foreground="Blue"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Margin="0,5"/>

            <ProgressBar Minimum="0" Maximum="{Binding TotalCount}" Value="{Binding CurrentProgress}" Height="20" />

            <TextBlock HorizontalAlignment="Center" Margin="0,5,0,0" FontSize="12" FontWeight="SemiBold">
        <Run Text="진행률: "/>
        <Run Text="{Binding CurrentProgress}"/>
        <Run Text=" / "/>
        <Run Text="{Binding TotalCount}"/>
        <Run Text=" (" Foreground="Gray"/>
        <Run Text="{Binding ProgressPercent}" Foreground="Red"/>
        <Run Text="%)" Foreground="Gray"/>
            </TextBlock>

        </StackPanel>
    </Grid>
</Window>
